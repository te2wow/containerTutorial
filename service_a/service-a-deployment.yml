apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-a
  namespace: tetsuwo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-a
  template:
    metadata:
      labels:
        app: service-a
    spec:
      containers:
      - name: service-a-container
        image: nginx:1.27.0 
        ports:
        - containerPort: 80
